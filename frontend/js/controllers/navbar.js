(function () {

  angular
    .module('autonomappr_app')
    .controller('navbarCtrl', navbarCtrl);

  navbarCtrl.$inject = ['$scope', '$rootScope', '$http', '$route', '$location'];

  function navbarCtrl($scope, $rootScope, $http, $route, $location) {
        
  }

})();
